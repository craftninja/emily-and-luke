<h1 class='page-header'><%= @photo.date.strftime("%B %Y") %> - <%= @photo.caption %></h1>

<div class="row">
  <div class="col-md-12">
    <%= image_tag @photo.img_url, class: 'img-responsive' %>
  </div>
</div>
